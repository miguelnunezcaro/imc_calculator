import 'package:flutter/material.dart';

class AppColors {
  
  static const Color primary = Color(0xFF2f312a);
  static const Color secondary = Color(0xFF2f312a);
  static const Color accent = Color(0xFF386663);


  //Backgrounds 

  static const Color background = Color(0xFF1a1c16);
  static const Color backgroundComponent = Color(0xFF2f312a);
  static const Color backgroundComponentSelected = Color(0xFF2f312a);

  //Fonts 

  static const Color primaryTitle = Colors.white;
  static const Color secondaryTitle = Color(0xFF386663);
  static const Color purpleTitle = Color(0xFF68548e);
}